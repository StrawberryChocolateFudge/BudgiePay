<!DOCTYPE html>
<html lang="en" data-theme="light">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"
    />
    <link rel="stylesheet" href="/stylesheets/pico.css" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Airdrop</title>
  </head>

  <body>
    <%- include('header'); -%>

    <main class="container">
      <div>
        <figure></figure>
        <div>
          <hgroup class="text-align-center">
            <h4 class="text-align-enter"><%= user.displayName %></h4>
          </hgroup>
        </div>
        <div>
          <% if (withdrawn) { %>
          <h5 class="text-align-center">
            You have withdrawn your tokens! Thank you for Joining!
          </h5>
          <button
            data-budgiecoinaddress="<%=budgiecoinaddress%>"
            id="add-tokens-to-wallet"
            class="airdropButton"
          >
            Add tokens to wallet
          </button>
          <%} else { %>
          <hgroup class="text-align-center">
            <h4>You have <%=followerscount %> Followers !</h4>
            <h4>Claim your Budgie Coins!</h4>
          </hgroup>
          <input
            class="max-width-300 center borderRadius-25"
            id="2fa-code"
            type="number"
            placeholder="Auth Code"
            required
          />
          <div id="errorSlot"></div>

          <button
            type="submit"
            data-budgiecoinaddress="<%=budgiecoinaddress%>"
            id="withdrawButton"
            class="airdropButton borderRadius-25"
          >
            <%=buttonMessage %>
          </button>
          <% } %>
        </div>
      </div>
    </main>
  </body>

  <script src="/js/web3.min.js"></script>
  <script src="/js/main.js"></script>
  <script src="/js/airdrop.js"></script>
</html>

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"
    />
    <link rel="stylesheet" href="/stylesheets/pico.css" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Airdrop</title>
    <style>
      :root:not([data-theme="dark"]),
      [data-theme="light"] {
        --nav-background: rgba(255, 255, 255, 0.7);
        --nav-border: rgba(115, 130, 140, 0.2);
      }

      [data-theme="dark"] {
        --nav-background: rgba(16, 24, 30, 0.8);
        --nav-border: rgba(115, 130, 140, 0.2);
      }

      main .grid > * {
        margin-bottom: var(--spacing);
      }
      @media (min-width: 992px) {
        main .grid {
          grid-column-gap: var(--spacing);
          grid-template-columns: 25% auto;
        }
      }
    </style>
  </head>

  <body>
    <%- include('header'); -%>

    <main class="container">
      <div>
        <figure></figure>
        <div>
          <hgroup class="text-align-center">
            <h4 class="text-align-enter"><%= user.displayName %></h4>
          </hgroup>
        </div>
        <div>
          <% if (withdrawn) { %>
          <h5 class="text-align-center">
            You have withdrawn your tokens! Thank you for Joining the DAO.
          </h5>
          <%} else { %>
          <hgroup class="text-align-center">
            <h4>You have <%=followerscount %> Followers !</h4>
          </hgroup>

          <button
            data-thefollowertokenaddress="<%=thefollowertokenaddress%>"
            data-signedargs="<%=signedargs%>"
            data-walletconnected="<%=walletconnected%>"
            data-signature="<%=signature%>"
            id="withdrawButton"
            class="airdropButton"
          >
            <%=buttonMessage %>
          </button>
          <% } %>
        </div>
      </div>
    </main>
  </body>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/web3/1.2.7-rc.0/web3.min.js"></script>
  <script src="/js/main.js"></script>
</html>
